name: deploy
on: [push]

jobs:
  update-ogcard-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Update ogcard.yaml
        id: update
        run: |
          ./tools/update-ogdata.bash
          updated=$(git diff --exit-code --quiet && echo 'false' || echo 'true')
          echo "updated=${updated}"
          echo "updated=${updated}" >>"${GITHUB_OUTPUT}"
      - name: Open or update PR if updated
        if: ${{ steps.update.outputs.updated }}
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git switch -c "ogcard/update-${BRANCH_NAME}"
          if git fetch origin "ogcard/update-${BRANCH_NAME}"; then
              git reset --hard FETCH_HEAD
              git add -A
              git commit --amend -m 'Update ogcard.yaml for ${{ github.sha }}'
          else
              git add -A
              git commit -m 'Update ogcard.yaml for ${{ github.sha }}'
              first=yes
          fi
          git push -f -u origin HEAD

          if [ "${first}" = yes ]; then
              echo 'create pr'
          fi
